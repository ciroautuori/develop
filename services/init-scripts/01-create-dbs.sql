-- Create Databases for all applications
CREATE DATABASE markettina;
CREATE DATABASE ironrep;
CREATE DATABASE studiocentos;
CREATE DATABASE iss_wbs;

-- Grant privileges (optional if using superuser, but good practice)
-- CREATE USER markettina WITH PASSWORD 'markettina2025';
-- GRANT ALL PRIVILEGES ON DATABASE markettina TO markettina;
-- ... (repeat for others if separate users desired, but single admin user is fine for now)
